name: Docker CI/CD

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-push:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Build and push server image
        uses: docker/build-push-action@v4
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: nitheesh2005/code-sync-server:latest

      - name: Build and push client image
        uses: docker/build-push-action@v4
        with:
          context: ./client
          file: ./client/Dockerfile
          push: true
          tags: nitheesh2005/code-sync-client:latest

      - name: Build and push worker image
        uses: docker/build-push-action@v4
        with:
          context: ./worker
          file: ./worker/Dockerfile
          push: true
          tags: nitheesh2005/code-sync-worker:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master' # deploy only on master branch
    steps:
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            # Pull latest images
            docker pull nitheesh2005/code-sync-server:latest
            docker pull nitheesh2005/code-sync-client:latest
            docker pull nitheesh2005/code-sync-worker:latest

            # Stop and remove existing containers if any (ignore errors)
            docker stop code-sync-server || true
            docker rm code-sync-server || true

            docker stop code-sync-client || true
            docker rm code-sync-client || true

            docker stop code-sync-worker || true
            docker rm code-sync-worker || true

            # Run containers with proper ports and options
            docker run -d --name code-sync-server -p 3000:3000 nitheesh2005/code-sync-server:latest

            docker run -d --name code-sync-client -p 5173:5173 nitheesh2005/code-sync-client:latest

            docker run -d --name code-sync-worker -p 4000:4000 nitheesh2005/code-sync-worker:latest
